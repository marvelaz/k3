apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: expense-tracker
data:
  VITE_API_URL: "http://backend-service:8000/api/v1"
  # For external access, you might want to use your ingress URL:
  # VITE_API_URL: "https://your-domain.com/api/v1"